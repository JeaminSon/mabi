[build]
  publish = "build" # 배포할 정적 파일이 있는 디렉토리
  command = "npm run build" # 빌드 명령

[dev]
  command = "npm start" # 개발 서버를 실행하는 명령
  port = 8888 # 포트 번호

[dev]
  command = "npm run start" # 또는 다른 명령어로, 당신의 app을 실행합니다.
  port = 8888 # 사용하고 싶은 포트 번호
